# Procedural Dialogue Generator Requirements
# Requires Ollama to be installed and running locally

# Core dependencies (standard library used primarily)
# No external pip packages required for basic operation

# For enhanced features (optional):
# numpy>=1.20.0  # For embedding calculations
# chromadb>=0.4.0  # For vector database
# sentence-transformers>=2.2.0  # For text embeddings